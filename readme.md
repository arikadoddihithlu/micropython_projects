


# GIT update
git config --global user.name "arikadoddihithlu"
git config --global user.email "arika.doddihithlu@gmail.com"
git add .
git commit -m "updated code"
git push -u origin main

# GIT Init
git init
git add .
git commit -m "First clean commit"
git branch -M main
git remote add origin https://github.com/arikadoddihithlu/ARIKA_esp32_projects.git
git push -u origin main --force